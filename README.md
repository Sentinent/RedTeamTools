# Red Team Tools
Random scripts that might be useful.

## Installation
`python3 -m venv .venv`

`source .venv/bin/activate`

`python3 -m pip install -r requirements.txt`

## Leave virtualenv
`deactivate`